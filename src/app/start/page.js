import Start from './components/Start'

export const metadata = {
  title: 'Get started',
}

const StartPage = () => {
  return (
    <main>
      <Start />
    </main>
  )
}

export default StartPage
